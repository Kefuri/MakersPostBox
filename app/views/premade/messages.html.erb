<!DOCTYPE html>
<html>
  <head>
    <title>Messages gift</title>
  </head>
  <body>
  <% @uri = request.env['PATH_INFO'] %>

  <div class="container">
    <script src='/app/assets/javascripts/application.js'></script>
    <h1>Gift of messages<i class="fas fa-mail"></i></h1>
    <p>Here are your cute messages!</p>
    <div>
      <a href= "/packages/new_messages" >Like this template? Click to make your package!</a>
    </div>
    <div class="gallery" id="gallery">
      <% @resources.each do | resource | %>
        <% if resource.link.include? "youtu" %>
          <% resource.link = "https://www.youtube.com/embed/#{resource.link[-11..-1]}"%>
          <div class="gallery-item">
            <div class="content">
              <iframe width="560" height="315" src=<%= resource.link %> frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen alt=<%= resource.alt_tag %>></iframe>
            </div>
          </div>
        <% else %>
          <div class="gallery-item">
            <div class="content">
              <img src=<%= resource.link %> alt=<%= resource.alt_tag %> >
            </div>
          </div>
        <% end %>
      <% end %>
    </div>

    <div class="bottom">
      <div>
        Icons made by <a href="https://www.flaticon.com/authors/smashicons" title="Smashicons">Smashicons</a> from <a href="https://www.flaticon.com/" title="Flaticon">www.flaticon.com</a>
      </div>
    </div>
  </body>
</html>
